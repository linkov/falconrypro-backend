## Customize the test machine
machine:


  # Version of ruby to use
  ruby:
    version:
      2.2.2

branches:
  only:
    - master


deployment:
  production:
    branch: master
    commands:
      - ./deploy_production.sh:
         timeout: 2200

dependencies:
  pre:
    - gem install bundler
